# openusd-overlay

Gentoo overlay skeleton for developing `media-gfx/openusd` (Pixar OpenUSD).

This repository is an ebuild repository (overlay). To enable it locally, copy the example from `repos.conf/openusd-overlay.conf.example` into `/etc/portage/repos.conf/` and adjust `location`.
